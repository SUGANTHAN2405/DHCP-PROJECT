# DHCP-PROJECT
DHCP (Dynamic Host Configuration Protocol) — a network protocol that automatically assigns IP address, subnet mask, gateway and DNS details to devices on a network.   It removes the need for manual configuration by a network admin, letting devices connect seamlessly while ensuring unique and valid network settings
